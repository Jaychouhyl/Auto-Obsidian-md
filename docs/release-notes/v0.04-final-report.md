# v0.04 发布记录

## 发布状态

- 仓库：`https://github.com/Jaychouhyl/Auto-Obsidian-md`
- 分支：`main`
- 标签：`v0.04`
- Release：`https://github.com/Jaychouhyl/Auto-Obsidian-md/releases/tag/v0.04`
- Windows Release workflow：`https://github.com/Jaychouhyl/Auto-Obsidian-md/actions/runs/28283061744`
- 结果：GitHub Actions 显示 `Windows Release` 成功，Release 已创建并带有安装包资产。

## 本地安装包

- 路径：`D:\obsidian-ingest-pipeline\desktop\src-tauri\target\release\bundle\nsis\Obsidian Ingest Studio_0.4.0_x64-setup.exe`
- 大小：`72,814,318` 字节，约 `69.44 MiB`
- SHA256：`8DF1BD13642B14282F26E83FC9E9B2C6E2F41A8BF5A55CF60425E557D1D66691`

## 已完成能力

- 桌面端新增统一账号中心，覆盖抖音、哔哩哔哩、YouTube、TikTok。
- 账号登录使用系统 Microsoft Edge 的独立 profile，不在项目配置里保存明文 Cookie。
- 采集任务运行时临时导出 Cookie，任务结束后清理临时文件。
- 抖音收藏夹采集接入当前账号，已验证当前账号为 `忆霖 / 60185413619`。
- 哔哩哔哩、YouTube、TikTok 已接入账号入口和鉴权路径；实际采集前仍需要用户先在独立网页登录对应账号。
- Docker Compose 可构建并启动 `auto-obsidian-md:local`，容器内 `doctor` 可执行。

## 验证结果

- Python：`D:\python\python.exe -m unittest discover -s tests`，83 个测试通过。
- 本机 doctor：必要项通过，`funasr` 为可选警告。
- 前端：`npm run build` 通过。
- Rust：`cargo fmt --check`、`cargo check`、`cargo test` 通过。
- Docker：`docker compose ps` 显示 `obsidian-ingest-pipeline-console-1` 正在运行；镜像 `auto-obsidian-md:local` 已构建。
- Docker doctor：容器内配置、队列目录、缓存目录、vault、yt-dlp、ffmpeg、LLM key 检查通过；抖音下载器、Whisper、FunASR 为容器内可选警告。
- 队列：当前本机队列 24 条均为 `done`，无 pending/failed。
- 临时文件：`cache/account-cookies` 计数为 0，缓存目录未残留 `.douyin-config.yml`。
- 凭据扫描：提交内容未命中真实 Cookie/API key 形态；模板、源码字段名和测试假值不算凭据泄露。

## 客观边界

- Windows 安装包已经可由 Release 下载，但新机器仍需要安装或配置外部依赖：系统 Edge、ffmpeg、yt-dlp、抖音下载器、Whisper/FunASR 或可替代转写工具，以及 DeepSeek API key。
- GitHub Release 的安装包由 Actions 重新构建，大小和哈希可能与本机安装包不同；本机哈希只对应本机生成的 exe。
- 账号登录态不随 GitHub 仓库发布。换机器或换账号时，需要在软件的“账号”页面重新登录并确认账号。

---

自动化写的  
署名：小黄狗
